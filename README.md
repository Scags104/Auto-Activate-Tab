# Auto-Activate-Tab
Extension for Chrome/Brave where when middle clicking or Ctrl+Click, the new tab is activated immediately rather than finding and clicking it.
Option to enable this only when Shift is pressed
